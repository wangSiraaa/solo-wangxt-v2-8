# Tests drive recall batches explicitly via RecallService; the scheduled processor
# would otherwise settle rows in the background and race the deterministic assertions.
app.recall.processor-delay-ms: 3600000
